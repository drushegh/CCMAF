# {{project-name}}

This project runs the **CCMAF framework, v2 line** — served by the `ccmaf`
plugin (with `ccmaf-kernel` as the safety floor). The framework's standing
rules arrive automatically as session context from the plugin; there is no
framework include to keep in this file. Project scaffolding and feature
opt-ins are managed by `/ccmaf:init` and `/ccmaf:update`.

## Project notes

<!-- Everything below is yours: stack notes, run commands, conventions,
     domain context. The framework never touches this file after init. -->

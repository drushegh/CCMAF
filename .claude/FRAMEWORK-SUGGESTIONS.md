# Framework Improvement Suggestions

These are ideas for improving the multi-agent framework ITSELF (not this
project). They are NOT implemented here — they are meant to flow back to the
framework so every project that uses it benefits.

> **Adopted this framework? Please send these upstream.** This file lives only
> in your project, so the framework's maintainers never see what your agents
> record here. To contribute an idea back, open an issue or pull request on the
> framework's upstream repository — its URL is the `FRAMEWORK_UPSTREAM_URL` in
> `.claude/.framework-version`. Pasting an entry from below is enough; the more
> context, the better. (Nothing here is sent anywhere automatically — it is
> yours to share or keep.)

<!-- Any agent can add a suggestion at any time — don't wait for session end. -->
<!-- Include enough context that a human reading this later can understand the problem and the proposed solution. -->
<!-- Newest entries at the top. -->
<!-- The test: "Could something different in the framework have prevented this problem?" -->
